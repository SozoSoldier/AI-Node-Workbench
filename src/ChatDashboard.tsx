import React from "react";
import { LiveAiStream } from "./LiveAiStream";

export const ChatDashboard: React.FC = () => {
  return <LiveAiStream />; // Renders directly into the layout's <main> slot
};
